# Third-party notices

`src/core.mjs` is a modified JavaScript adaptation of portions of `src/state.ts` and `src/compact.ts` in tamaratran/fast-jev-compaction. Original repository: https://github.com/tamaratran/fast-jev-compaction . Pinned source revision and blob hashes are in `third_party/UPSTREAM.json`.

The original MIT notice is reproduced in `third_party/fast-jev-compaction.LICENSE`. Preserve it when distributing this bundle or derivative versions. Eutrya-specific archive, transport, runtime integration, policy safeguards and tests are modifications; they are not represented as upstream implementation or endorsement.

See docs/UPSTREAM.md for the adaptation boundary and differences. No upstream Claude Code plugin, compiled UI, font, API credential or complete Eutrya application is redistributed in this bundle.
