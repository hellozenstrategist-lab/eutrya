# Never put a real key in a committed file.
# Eutrya does not automatically load .env files from arbitrary repositories.
# Use exported environment variables, or explicitly launch Node with --env-file.
AI_GATEWAY_API_KEY=replace-locally
